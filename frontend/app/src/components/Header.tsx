import { styled } from "styled-components";

const HeaderSyle = styled.header`
  position: fixed;
  top: 0;
  height: 64px;
  width: 100%;
  background: #FBF9FA;
  display: flex;
`;

export const Header: React.FC = () => {
  return (
    <HeaderSyle >

    </HeaderSyle>
  );
}
